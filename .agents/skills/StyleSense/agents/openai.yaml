interface:
  display_name: "StyleSense"
  short_description: "Repo-specific patterns and workflows for StyleSense"
  default_prompt: "Use the StyleSense repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true